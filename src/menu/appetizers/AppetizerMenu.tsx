import React from "react";
// import { useNavigate } from "react-router";
import axios from "axios";

// type Appetizer = {
// 	name: { value: string };
// 	price: { value: number };
// } & EventTarget;

const AppetizerMenu = () => {
	const getAllAppetizers = () => {
		// axios.get("http://localhost:8080/appetizer_menu").then(response => {
		// 	// const allAppetizers = response.data.;
		// });
	};

	return (
		<div>
			<h1>Appetizer Menu </h1>
		</div>
	);
};

export default AppetizerMenu;
